









































/* const getData = (url, callback) => {
    fetch(url)
        .then((response) =>
            response.json()
        )
        .then((students) => {
            showInTable(students);
        })
        .catch((err) => {
            console.log(err);
        });
};
getData(url); */
